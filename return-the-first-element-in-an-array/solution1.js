'use strict';

/**
 * Retrieves the first element of an array
 * @param {array} array - The input array from which to retrieve the first element.
 * @returns {*} The first element of the array.
 */

export function getFirstValue(arr) {
    return arr[0];
}
